diff --git a/README.md b/README.md
index dcb5d597ccf48910877eef22c994cf494e93642f..bcc05cec5120d23dd0de6fc354e814fcf045462d 100644
--- a/README.md
+++ b/README.md
@@ -30,11 +30,22 @@ flowchart TD
 
 ## Carpetas
 
-- `apps/web`: Angular 21 standalone, bandejas por estado, revisiÃ³n y calendario.
+- `apps/web`: Angular 21 standalone con mÃ³dulos funcionales lazy-loaded, bandejas por estado, revisiÃ³n y calendario.
 - `apps/api`: NestJS, OpenAI, OAuth/PublicaciÃ³n TikTok, scheduler y FFmpeg.
 - `packages/contracts`: contratos compartidos frontend/backend.
 - `supabase/migrations`: tablas, Ã­ndices, RLS, buckets privados y auditorÃ­a.
 
+### MÃ³dulos del frontend
+
+| Ruta | MÃ³dulo | Responsabilidad |
+|---|---|---|
+| `/publications` | Publicaciones | Crear paquetes con IA, revisar, aprobar y programar piezas |
+| `/packages` | Paquetes | Consultar lotes existentes y su historial de imÃ¡genes |
+| `/music` | MÃºsica | Subir, validar y previsualizar pistas licenciadas |
+| `/settings/tiktok` | Cuenta TikTok | Conectar, consultar y desconectar la cuenta de publicaciÃ³n |
+
+El formulario **Crear paquete con IA** existe Ãºnicamente en Publicaciones. El estado compartido estÃ¡ centralizado en `WorkspaceStore`, mientras cada mÃ³dulo se descarga bajo demanda mediante Angular Router.
+
 ## Requisitos
 
 - Node.js 22+
diff --git a/apps/web/src/app/app.component.html b/apps/web/src/app/app.component.html
index 8c50291e1557efab66cda0268c1adba7d64c8a3f..fb613e185686a362a3912f7203f3eda58b54736c 100644
--- a/apps/web/src/app/app.component.html
+++ b/apps/web/src/app/app.component.html
@@ -21,14 +21,15 @@
         type="email"
         [(ngModel)]="email"
         placeholder="tu@correo.com"
+        (keyup.enter)="emailValid() && login()"
       />
       <button
         class="primary"
-        [disabled]="busy() || !emailValid()"
-        [attr.aria-busy]="isLoading('login')"
+        [disabled]="workspace.busy() || !emailValid()"
+        [attr.aria-busy]="workspace.isLoading('login')"
         (click)="login()"
       >
-        @if (isLoading("login")) {
+        @if (workspace.isLoading("login")) {
           <span class="spinner" aria-hidden="true"></span> Enviando enlaceâ€¦
         } @else {
           Recibir enlace de acceso
@@ -42,33 +43,27 @@
       <div class="brand">
         <span>âœ¦</span><strong>Impulso <b>IA</b></strong>
       </div>
-      <nav>
-        <button
-          [class.active]="view() === 'publications'"
-          [disabled]="busy()"
-          (click)="navigate('publications')"
-        >
+      <nav aria-label="MÃ³dulos principales">
+        <a routerLink="/publications" routerLinkActive="active">
           â–¤ Publicaciones
-        </button>
-        <button
-          [class.active]="view() === 'packages'"
-          [disabled]="busy()"
-          (click)="navigate('packages')"
-        >
+        </a>
+        <a routerLink="/packages" routerLinkActive="active">
           â–¦ Paquetes de imÃ¡genes
-        </button>
-        <button [class.active]="view() === 'music'" [disabled]="busy()" (click)="navigate('music')">â™« MÃºsica</button>
-        <button [class.active]="view() === 'tiktok'" [disabled]="busy()" (click)="navigate('tiktok')">â—‰ Cuenta TikTok</button>
+        </a>
+        <a routerLink="/music" routerLinkActive="active">â™« MÃºsica</a>
+        <a routerLink="/settings/tiktok" routerLinkActive="active">
+          â—‰ Cuenta TikTok
+        </a>
       </nav>
       <div class="account">
-        <span>{{ auth.user()?.email?.slice(0, 2)?.toUpperCase() }}</span
-        ><small>{{ auth.user()?.email }}</small
-        ><button
-          [disabled]="busy()"
-          [attr.aria-busy]="isLoading('logout')"
+        <span>{{ auth.user()?.email?.slice(0, 2)?.toUpperCase() }}</span>
+        <small>{{ auth.user()?.email }}</small>
+        <button
+          [disabled]="workspace.busy()"
+          [attr.aria-busy]="workspace.isLoading('logout')"
           (click)="logout()"
         >
-          @if (isLoading("logout")) {
+          @if (workspace.isLoading("logout")) {
             <span class="spinner spinner-dark" aria-hidden="true"></span>
           } @else {
             Salir
@@ -78,414 +73,18 @@
     </aside>
 
     <section class="workspace">
-      <header>
-        <div>
-          <p class="eyebrow">CENTRO DE PUBLICACIONES</p>
-          <h1>Contenido con control humano.</h1>
-          <p>
-            La IA crea el fondo y la frase. TÃº decides quÃ© se publica y cuÃ¡ndo.
-          </p>
-        </div>
-        <button
-          class="refresh"
-          [disabled]="busy()"
-          [attr.aria-busy]="isLoading('load')"
-          (click)="load()"
-        >
-          @if (isLoading("load")) {
-            <span class="spinner spinner-dark" aria-hidden="true"></span>
-            Actualizandoâ€¦
-          } @else {
-            â†» Actualizar
-          }
-        </button>
-      </header>
-
-      <section class="generator">
-        <div>
-          <span class="step">1</span>
-          <div>
-            <h2>Crear paquete con IA</h2>
-            <p>
-              Generamos frases, fondos verticales y las imÃ¡genes finales listas
-              para aprobaciÃ³n.
-            </p>
-          </div>
-        </div>
-        <div class="fields">
-          <label
-            >Nombre del paquete<input
-              [(ngModel)]="batchName"
-              placeholder="Ej. MotivaciÃ³n semanal"
-          /></label>
-          <label class="brand-field">Marca en la imagen<input [(ngModel)]="brandName" maxlength="32" placeholder="Ej. Impulso IA" /></label>
-          <label>Tema<input [(ngModel)]="theme" /></label>
-          <label
-            >Tono<select [(ngModel)]="tone">
-              <option value="cercano">Cercano y positivo</option>
-              <option value="energico">Directo y enÃ©rgico</option>
-              <option value="reflexivo">Reflexivo</option>
-            </select></label
-          >
-          <label
-            >Cantidad<select [(ngModel)]="count">
-              <option [ngValue]="2">2 publicaciones</option>
-              <option [ngValue]="3">3 publicaciones</option>
-            </select></label
-          >
-          <button
-            class="primary"
-            [disabled]="busy()"
-            [attr.aria-busy]="isLoading('generate')"
-            (click)="generate()"
-          >
-            @if (isLoading("generate")) {
-              <span class="spinner" aria-hidden="true"></span> Generando
-              imÃ¡genesâ€¦
-            } @else {
-              âœ¦ Generar paquete
-            }
-          </button>
-        </div>
-      </section>
-
-      @if (generationProgress(); as progress) {
-        <section class="live-generation" aria-live="polite">
-          <div><span class="spinner spinner-dark"></span><strong>Creando imagen {{ progress.current + 1 > progress.total ? progress.total : progress.current + 1 }} de {{ progress.total }}</strong></div>
-          <div class="live-bar"><span [style.width.%]="(progress.current / progress.total) * 100"></span></div>
-          <small>Cada resultado aparece en el paquete apenas termina, sin dejar la pantalla esperando a que concluya todo el lote.</small>
-        </section>
-      }
-
-      @if (view() === "music") {
-        <section class="integration-page">
-          <div class="section-title"><div><p class="eyebrow">BIBLIOTECA DE AUDIO</p><h2>MÃºsica verificada</h2></div><small>{{ musicTracks().length }} pistas activas</small></div>
-          <div class="integration-grid">
-            <article class="panel upload-panel">
-              <h3>AÃ±adir una pista</h3>
-              <p>Sube mÃºsica original, licenciada o generada con IA. FFprobe valida el formato y la duraciÃ³n antes de habilitarla.</p>
-              <label>Nombre<input [(ngModel)]="musicName" placeholder="Ej. Amanecer sereno" /></label>
-              <label>Origen<select [(ngModel)]="musicSource"><option value="uploaded">Original o licenciada</option><option value="ai_generated">Generada con IA</option></select></label>
-              @if (musicSource === "ai_generated") { <label>Proveedor de IA<input [(ngModel)]="musicAiProvider" placeholder="Ej. Suno, Udioâ€¦" /></label> }
-              <label>Licencia / permiso<textarea [(ngModel)]="musicLicense" placeholder="Describe el permiso de uso comercial"></textarea></label>
-              <label class="file-field">Archivo MP3, WAV o M4A<input type="file" accept="audio/mpeg,audio/wav,audio/mp4,.m4a" (change)="chooseMusicFile($event)" /></label>
-              <button class="primary" [disabled]="busy()" (click)="uploadMusic()">@if (isLoading('musicUpload')) { <span class="spinner"></span> Verificandoâ€¦ } @else { Verificar y guardar pista }</button>
-            </article>
-            <div class="track-list">
-              @for (track of musicTracks(); track track.id) {
-                <article class="track-card">
-                  <div class="track-icon">â™«</div><div><strong>{{ track.name }}</strong><small>{{ track.durationSeconds }} s Â· {{ track.source === 'ai_generated' ? 'IA: ' + track.aiProvider : 'Original/licenciada' }}</small></div>
-                  <span class="verified">âœ“ Verificada</span>
-                  @if (track.previewUrl) { <audio controls preload="none" [src]="track.previewUrl"></audio> }
-                  <p>{{ track.licenseNotes }}</p>
-                </article>
-              } @empty { <div class="empty">No hay pistas utilizables. Sube la primera para poder programar.</div> }
-            </div>
-          </div>
-        </section>
-      } @else if (view() === "tiktok") {
-        <section class="integration-page">
-          <div class="section-title"><div><p class="eyebrow">INTEGRACIONES</p><h2>Cuenta TikTok</h2></div></div>
-          <article class="panel tiktok-panel">
-            <div class="tiktok-mark">â™ª</div>
-            @if (!tiktok().configured) {
-              <h3>Falta configurar TikTok en el backend</h3><p>Agrega TIKTOK_CLIENT_KEY, TIKTOK_CLIENT_SECRET y TIKTOK_REDIRECT_URI. El secret nunca se envÃ­a al navegador.</p>
-            } @else if (tiktok().connected) {
-              <span class="verified">âœ“ Conectada</span><h3>{{ tiktok().displayName || 'Cuenta TikTok autorizada' }}</h3><p>La aplicaciÃ³n puede publicar piezas aprobadas. Los tokens se guardan cifrados y se renuevan automÃ¡ticamente.</p>
-              <button class="danger disconnect" [disabled]="busy()" (click)="disconnectTikTok()">Desconectar cuenta</button>
-            } @else {
-              <h3>Conecta la cuenta que publicarÃ¡</h3><p>Inicia OAuth con la cuenta de TikTok destino y autoriza los permisos de publicaciÃ³n.</p>
-              <button class="primary" [disabled]="busy()" (click)="connectTikTok()">@if (isLoading('tiktok')) { <span class="spinner"></span> Conectandoâ€¦ } @else { Conectar con TikTok }</button>
-            }
-          </article>
-        </section>
-      } @else if (view() === "packages") {
-        <section class="packages">
-          <div class="section-title">
-            <div>
-              <p class="eyebrow">PAQUETES GENERADOS</p>
-              <h2>Lotes de imÃ¡genes</h2>
-            </div>
-            <small>{{ batches().length }} paquetes</small>
-          </div>
-          <div class="package-board">
-            <div class="package-list">
-              @for (batch of batches(); track batch.id) {
-                <button
-                  class="package-card"
-                  [disabled]="busy()"
-                  [class.selected]="selectedBatch()?.id === batch.id"
-                  (click)="selectBatch(batch)"
-                >
-                  <div>
-                    <strong>{{ batch.name }}</strong
-                    ><small>{{
-                      batch.createdAt | date: "dd MMM, h:mm a"
-                    }}</small>
-                  </div>
-                  <span class="badge" [attr.data-status]="batch.status">{{
-                    batchStatusLabel(batch.status)
-                  }}</span>
-                  <div class="package-progress">
-                    <span
-                      [style.width.%]="
-                        batch.requestedCount
-                          ? (batch.generatedCount / batch.requestedCount) * 100
-                          : 0
-                      "
-                    ></span>
-                  </div>
-                  <small
-                    >{{ batch.generatedCount }} de
-                    {{ batch.requestedCount }} imÃ¡genes generadas</small
-                  >
-                </button>
-              } @empty {
-                <div class="empty">
-                  TodavÃ­a no has generado paquetes de imÃ¡genes.
-                </div>
-              }
-            </div>
-
-            <article class="package-detail">
-              @if (selectedBatch(); as batch) {
-                <div class="review-top">
-                  <div>
-                    <p class="eyebrow">DETALLE DEL PAQUETE</p>
-                    <h2>{{ batch.name }}</h2>
-                  </div>
-                  <span class="badge" [attr.data-status]="batch.status">{{
-                    batchStatusLabel(batch.status)
-                  }}</span>
-                </div>
-                <p class="package-description">
-                  {{ batch.theme }} Â· {{ batch.tone }} Â·
-                  {{ batch.publications.length }} publicaciones
-                  @if (batch.obsoleteCount) { Â· {{ batch.obsoleteCount }} versiones obsoletas archivadas }
-                </p>
-                <div class="package-images">
-                  @for (item of batch.publications; track item.id) {
-                    <button [disabled]="busy()" (click)="openPublication(item)">
-                      <div
-                        class="package-image"
-                        [style.background-image]="imageStyle(item)"
-                      >
-                        @if (!item.composedImageUrl) {
-                          <blockquote>â€œ{{ item.quote }}â€</blockquote>
-                        }
-                      </div>
-                      <span>{{ statusLabel(item.status) }}</span>
-                    </button>
-                  }
-                </div>
-                @if (batch.status === "pending_review") {
-                  <button
-                    class="approve approve-batch"
-                    [disabled]="busy()"
-                    [attr.aria-busy]="isLoading('approveBatch')"
-                    (click)="approveSelectedBatch()"
-                  >
-                    @if (isLoading("approveBatch")) {
-                      <span class="spinner" aria-hidden="true"></span>
-                      Aprobandoâ€¦
-                    } @else {
-                      âœ“ Aprobar paquete completo
-                    }
-                  </button>
-                }
-                <p class="safety">
-                  Puedes abrir cualquier imagen para aprobarla, rechazarla o
-                  regenerarla individualmente.
-                </p>
-              } @else {
-                <div class="empty review-empty">
-                  Selecciona un paquete para ver sus imÃ¡genes.
-                </div>
-              }
-            </article>
-          </div>
-        </section>
-      } @else {
-        <section class="inbox">
-          <div class="tabs">
-            @for (tab of tabs; track tab.value) {
-              <button
-                [disabled]="busy()"
-                [class.active]="filter() === tab.value"
-                (click)="filter.set(tab.value)"
-              >
-                {{ tab.label }} <b>{{ countStatus(tab.value) }}</b>
-              </button>
-            }
-          </div>
-          <div class="board">
-            <div class="cards">
-              @if (loading()) {
-                <div class="empty">Actualizando publicacionesâ€¦</div>
-              }
-              @for (item of filtered(); track item.id) {
-                <button
-                  class="publication-card"
-                  [disabled]="busy()"
-                  [class.selected]="selected()?.id === item.id"
-                  (click)="select(item)"
-                >
-                  <div
-                    class="thumb"
-                    [style.background-image]="imageStyle(item)"
-                  >
-                    <span class="badge" [attr.data-status]="item.status">{{
-                      statusLabel(item.status)
-                    }}</span>
-                    @if (!item.composedImageUrl) {
-                      <blockquote>â€œ{{ item.quote }}â€</blockquote>
-                    }
-                  </div>
-                  <div class="card-copy">
-                    <strong>{{ item.theme }}</strong
-                    ><small>{{
-                      item.scheduledFor
-                        ? (item.scheduledFor | date: "dd MMM, h:mm a")
-                        : "Sin fecha programada"
-                    }}</small>
-                  </div>
-                </button>
-              } @empty {
-                @if (!loading()) {
-                  <div class="empty">No hay publicaciones en esta bandeja.</div>
-                }
-              }
-            </div>
-
-            <article class="review">
-              @if (selected(); as item) {
-                <div class="review-top">
-                  <div>
-                    <p class="eyebrow">REVISIÃ“N</p>
-                    <h2>{{ statusLabel(item.status) }}</h2>
-                  </div>
-                  <span class="badge" [attr.data-status]="item.status">{{
-                    statusLabel(item.status)
-                  }}</span>
-                </div>
-                <div class="phone" [style.background-image]="imageStyle(item)">
-                  @if (!item.composedImageUrl) {
-                    <div class="phone-brand">âœ¦ {{ item.brandName }}</div>
-                    <blockquote>â€œ{{ item.quote }}â€</blockquote>
-                  }
-                  <small>â™« MÃºsica seleccionada al programar</small>
-                </div>
-                <label>Frase generada</label>
-                <p class="generated-text">{{ item.quote }}</p>
-                <label>DescripciÃ³n</label>
-                <p class="caption">{{ item.caption }}</p>
-                <div class="hashtags">
-                  @for (tag of item.hashtags; track tag) {
-                    <span>{{ tag }}</span>
-                  }
-                </div>
-                @if (item.status === "pending_review") {
-                  <div class="actions">
-                    <button
-                      class="secondary"
-                      [disabled]="busy()"
-                      [attr.aria-busy]="isLoading('regenerate')"
-                      (click)="act('regenerate')"
-                    >
-                      @if (isLoading("regenerate")) {
-                        <span
-                          class="spinner spinner-dark"
-                          aria-hidden="true"
-                        ></span>
-                      } @else {
-                        â†» Regenerar
-                      }
-                    </button>
-                    <button
-                      class="danger"
-                      [disabled]="busy()"
-                      [attr.aria-busy]="isLoading('reject')"
-                      (click)="act('reject')"
-                    >
-                      @if (isLoading("reject")) {
-                        <span
-                          class="spinner spinner-dark"
-                          aria-hidden="true"
-                        ></span>
-                      } @else {
-                        Rechazar
-                      }
-                    </button>
-                    <button
-                      class="approve"
-                      [disabled]="busy()"
-                      [attr.aria-busy]="isLoading('approve')"
-                      (click)="act('approve')"
-                    >
-                      @if (isLoading("approve")) {
-                        <span class="spinner" aria-hidden="true"></span>
-                      } @else {
-                        âœ“ Aprobar
-                      }
-                    </button>
-                  </div>
-                }
-                @if (item.status === "approved") {
-                  <div class="schedule-box">
-                    <h3>Programar publicaciÃ³n</h3>
-                    <label
-                      >Fecha y hora<input
-                        type="datetime-local"
-                        [(ngModel)]="scheduledFor" /></label
-                    ><label
-                      >MÃºsica<select [(ngModel)]="musicTrackId">
-                        <option value="">
-                          Selecciona una pista licenciada
-                        </option>
-                        @for (track of musicTracks(); track track.id) { <option [value]="track.id">{{ track.name }} Â· {{ track.durationSeconds }} s</option> }
-                      </select></label
-                    >
-                    @if (musicTrackId) {
-                      @for (track of musicTracks(); track track.id) { @if (track.id === musicTrackId && track.previewUrl) { <audio class="schedule-audio" controls preload="none" [src]="track.previewUrl"></audio> } }
-                    }
-                    <div class="schedule-checks"><span [class.ok]="musicTracks().length">{{ musicTracks().length ? 'âœ“ MÃºsica verificada' : '! Falta mÃºsica' }}</span><span [class.ok]="tiktok().connected">{{ tiktok().connected ? 'âœ“ TikTok conectado' : '! Falta TikTok' }}</span></div>
-                    <button
-                      class="primary"
-                      [disabled]="busy() || !tiktok().connected || !musicTracks().length"
-                      [attr.aria-busy]="isLoading('schedule')"
-                      (click)="schedule()"
-                    >
-                      @if (isLoading("schedule")) {
-                        <span class="spinner" aria-hidden="true"></span>
-                        Programandoâ€¦
-                      } @else {
-                        Programar para TikTok
-                      }
-                    </button>
-                  </div>
-                }
-                <p class="safety">
-                  ðŸ”’ Nunca se programa ni publica sin aprobaciÃ³n registrada.
-                </p>
-              } @else {
-                <div class="empty review-empty">
-                  Selecciona una publicaciÃ³n para revisarla.
-                </div>
-              }
-            </article>
-          </div>
-        </section>
-      }
+      <router-outlet />
     </section>
   </main>
 }
-@if (message()) {
+
+@if (workspace.message()) {
   <div
     class="toast"
-    [attr.data-kind]="messageKind()"
+    [attr.data-kind]="workspace.messageKind()"
     role="status"
     aria-live="polite"
   >
-    {{ message() }}
+    {{ workspace.message() }}
   </div>
 }
diff --git a/apps/web/src/app/app.component.scss b/apps/web/src/app/app.component.scss
index d3bf89da27589e5ab9c33d4320ce0bde283e206a..ebb708ce4a4361e6dede79925be4a095bbd4ebee 100644
--- a/apps/web/src/app/app.component.scss
+++ b/apps/web/src/app/app.component.scss
@@ -1,4 +1,4 @@
-:host {
+app-root {
   display: block;
   min-height: 100vh;
   color: #252a27;
@@ -115,15 +115,16 @@ nav {
   display: grid;
   gap: 7px;
 }
-nav button {
+nav a {
   border: 0;
   background: transparent;
   text-align: left;
   padding: 11px 12px;
   border-radius: 10px;
   color: #68706b;
+  text-decoration: none;
 }
-nav button.active {
+nav a.active {
   background: #d8e6db;
   color: #254f3d;
   font-weight: 750;
@@ -537,7 +538,7 @@ header > div > p:last-child {
 }
 
 /* Identidad visual: mÃ¡s contraste, profundidad y acentos cÃ¡lidos. */
-:host {
+app-root {
   --ink: #17231f;
   --forest: #123f32;
   --forest-2: #1e634b;
@@ -553,9 +554,9 @@ header > div > p:last-child {
 .shell > aside { background: linear-gradient(180deg, #123f32, #0b2821); border: 0; color: white; box-shadow: 10px 0 35px #0b281f20; }
 .brand b { color: #f0bf62; }
 .brand > span { background: var(--gold); color: #18382c; }
-nav button { color: #d5e5dc; }
-nav button:hover:not(:disabled) { background: #ffffff12; color: #fff; }
-nav button.active { background: #f1bd5b; color: #18382c; box-shadow: 0 8px 20px #00000024; }
+nav a { color: #d5e5dc; }
+nav a:hover { background: #ffffff12; color: #fff; }
+nav a.active { background: #f1bd5b; color: #18382c; box-shadow: 0 8px 20px #00000024; }
 .account { border-color: #ffffff24; }
 .account > span { background: #f1bd5b; color: #17372c; }
 .account button, .account small { color: #d8e6df; }
@@ -603,7 +604,7 @@ textarea { min-height: 88px; padding: 11px 12px; resize: vertical; border: 1px s
 @media (max-width: 760px) {
   .shell > aside { position: static; }
   .shell nav { display: flex; overflow-x: auto; margin-top: 14px; }
-  .shell nav button { white-space: nowrap; }
+  .shell nav a { white-space: nowrap; }
   .account { display: grid; }
   .track-card { grid-template-columns: auto 1fr; }
   .track-card .verified { grid-column: 2; justify-self: start; }
diff --git a/apps/web/src/app/app.component.ts b/apps/web/src/app/app.component.ts
index ec435b889bcf987536208552434489996a88a7bb..3e42ffe36aa622d70bfb5450f31545c33ff728fd 100644
--- a/apps/web/src/app/app.component.ts
+++ b/apps/web/src/app/app.component.ts
@@ -1,391 +1,44 @@
-import { Component, computed, inject, signal } from "@angular/core";
 import { CommonModule } from "@angular/common";
+import { Component, ViewEncapsulation, inject } from "@angular/core";
 import { FormsModule } from "@angular/forms";
-import { HttpErrorResponse } from "@angular/common/http";
-import type {
-  Publication,
-  PublicationBatch,
-  PublicationBatchStatus,
-  PublicationStatus,
-  MusicTrack,
-  TikTokConnectionStatus,
-} from "@impulso/contracts";
+import { RouterLink, RouterLinkActive, RouterOutlet } from "@angular/router";
 import { AuthService } from "./auth.service";
-import { PublicationsApiService } from "./publications-api.service";
-import { IntegrationsApiService } from "./integrations-api.service";
-
-type Filter = "all" | PublicationStatus;
-type View = "publications" | "packages" | "music" | "tiktok";
-type AsyncAction =
-  | "login"
-  | "logout"
-  | "load"
-  | "generate"
-  | "regenerate"
-  | "reject"
-  | "approve"
-  | "approveBatch"
-  | "schedule"
-  | "musicUpload"
-  | "tiktok";
-type MessageKind = "success" | "error";
+import { WorkspaceStore } from "./workspace.store";
 
 @Component({
   selector: "app-root",
   standalone: true,
-  imports: [CommonModule, FormsModule],
+  imports: [
+    CommonModule,
+    FormsModule,
+    RouterLink,
+    RouterLinkActive,
+    RouterOutlet,
+  ],
   templateUrl: "./app.component.html",
   styleUrl: "./app.component.scss",
+  encapsulation: ViewEncapsulation.None,
 })
 export class AppComponent {
   readonly auth = inject(AuthService);
-  private readonly api = inject(PublicationsApiService);
-  private readonly integrations = inject(IntegrationsApiService);
-  readonly items = signal<Publication[]>([]);
-  readonly batches = signal<PublicationBatch[]>([]);
-  readonly loading = signal(false);
-  readonly pendingActions = signal<ReadonlySet<AsyncAction>>(
-    new Set<AsyncAction>(),
-  );
-  readonly busy = computed(() => this.pendingActions().size > 0);
-  readonly filter = signal<Filter>("pending_review");
-  readonly view = signal<View>("publications");
-  readonly selected = signal<Publication | null>(null);
-  readonly selectedBatch = signal<PublicationBatch | null>(null);
-  readonly musicTracks = signal<MusicTrack[]>([]);
-  readonly tiktok = signal<TikTokConnectionStatus>({ configured: false, connected: false });
-  readonly generationProgress = signal<{ current: number; total: number } | null>(null);
-  readonly message = signal("");
-  readonly messageKind = signal<MessageKind>("success");
-  readonly filtered = computed(() =>
-    this.filter() === "all"
-      ? this.items()
-      : this.items().filter((item) => item.status === this.filter()),
-  );
+  readonly workspace = inject(WorkspaceStore);
   email = "";
-  batchName = "";
-  brandName = "IMPULSO IA";
-  theme = "MotivaciÃ³n diaria";
-  tone: "cercano" | "energico" | "reflexivo" = "cercano";
-  count: 2 | 3 = 3;
-  scheduledFor = "";
-  musicTrackId = "";
-  musicName = "";
-  musicLicense = "";
-  musicSource: "uploaded" | "ai_generated" = "uploaded";
-  musicAiProvider = "";
-  musicFile: File | null = null;
-  private messageTimer?: ReturnType<typeof setTimeout>;
-  readonly tabs: { value: Filter; label: string }[] = [
-    { value: "pending_review", label: "Pendientes" },
-    { value: "approved", label: "Aprobadas" },
-    { value: "scheduled", label: "Programadas" },
-    { value: "published", label: "Publicadas" },
-    { value: "failed", label: "Con error" },
-    { value: "obsolete", label: "Obsoletas" },
-    { value: "all", label: "Todas" },
-  ];
 
   constructor() {
     void this.auth.ready.then(() => {
-      if (this.auth.user()) void this.initializeWorkspace();
+      if (this.auth.user()) void this.workspace.initializeWorkspace();
     });
   }
-  countStatus(value: Filter) {
-    return value === "all"
-      ? this.items().length
-      : this.items().filter((item) => item.status === value).length;
-  }
+
   emailValid() {
     return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(this.email.trim());
   }
-  isLoading(action: AsyncAction) {
-    return this.pendingActions().has(action);
-  }
-  async login() {
-    if (!this.emailValid())
-      return this.flash("Ingresa un correo electrÃ³nico vÃ¡lido.", "error");
-    await this.withLoading("login", async () => {
-      try {
-        await this.auth.sendMagicLink(this.email.trim());
-        this.flash(
-          "Te enviamos un enlace de acceso. Revisa tu bandeja de entrada y el correo no deseado.",
-        );
-      } catch (error) {
-        this.flash(this.errorText(error), "error");
-      }
-    });
-  }
-  async logout() {
-    await this.withLoading("logout", async () => {
-      try {
-        await this.auth.signOut();
-        this.items.set([]);
-        this.batches.set([]);
-        this.selected.set(null);
-        this.selectedBatch.set(null);
-      } catch (error) {
-        this.flash(this.errorText(error), "error");
-      }
-    });
-  }
-  async load() {
-    await this.withLoading("load", async () => {
-      this.loading.set(true);
-      try {
-        const [rows, batches] = await Promise.all([
-          this.api.list(),
-          this.api.listBatches(),
-        ]);
-        this.items.set(rows);
-        this.batches.set(batches);
-        if (!this.selected() && rows.length) this.selected.set(rows[0]);
-        if (!this.selectedBatch() && batches.length)
-          this.selectedBatch.set(batches[0]);
-      } catch (error) {
-        this.flash(this.errorText(error), "error");
-      } finally {
-        this.loading.set(false);
-      }
-    });
-  }
-  async initializeWorkspace() {
-    await this.consumeTikTokCallback();
-    await Promise.all([this.load(), this.loadIntegrations()]);
-  }
 
-  async loadIntegrations() {
-    try {
-      const [music, tiktok] = await Promise.all([this.integrations.music(), this.integrations.tiktokStatus()]);
-      this.musicTracks.set(music);
-      this.tiktok.set(tiktok);
-      if (!this.musicTrackId && music.length) this.musicTrackId = music[0].id;
-    } catch (error) { this.flash(this.errorText(error), "error"); }
-  }
-  async generate() {
-    await this.withLoading("generate", async () => {
-      try {
-        let batch = await this.api.generate({
-          name: this.batchName.trim() || undefined,
-          brandName: this.brandName.trim() || "IMPULSO IA",
-          theme: this.theme,
-          tone: this.tone,
-          count: this.count,
-        });
-        this.view.set("packages");
-        this.generationProgress.set({ current: 0, total: this.count });
-        this.batches.update((rows) => [batch, ...rows]);
-        this.selectedBatch.set(batch);
-        for (let current = 1; current <= this.count; current++) {
-          const result = await this.api.generateNext(batch.id);
-          batch = result.batch;
-          this.generationProgress.set({ current, total: this.count });
-          this.batches.update((rows) => [batch, ...rows.filter((row) => row.id !== batch.id)]);
-          this.selectedBatch.set(batch);
-          this.items.update((rows) => [result.publication, ...rows]);
-        }
-        this.generationProgress.set(null);
-        await this.load();
-        this.selectedBatch.set(
-          this.batches().find((item) => item.id === batch.id) ?? batch,
-        );
-        this.filter.set("pending_review");
-        this.flash(
-          "La IA creÃ³ las imÃ¡genes del paquete. Revisa cada publicaciÃ³n.",
-        );
-      } catch (error) {
-        this.generationProgress.set(null);
-        this.flash(this.errorText(error), "error");
-      }
-    });
-  }
-  async act(action: "approve" | "reject" | "regenerate") {
-    const item = this.selected();
-    if (!item) return;
-    await this.withLoading(action, async () => {
-      try {
-        await this.api[action](item.id);
-        await this.load();
-        this.selected.set(action === "regenerate"
-          ? this.items().find((row) => row.supersedesId === item.id) ?? this.items().find((row) => row.id === item.id) ?? null
-          : this.items().find((row) => row.id === item.id) ?? null);
-        this.flash(
-          action === "approve"
-            ? "Contenido aprobado."
-            : action === "reject"
-              ? "Contenido rechazado."
-              : "Nueva versiÃ³n generada.",
-        );
-      } catch (error) {
-        this.flash(this.errorText(error), "error");
-      }
-    });
-  }
-  navigate(view: View) {
-    this.view.set(view);
-    if (view === "music" || view === "tiktok") void this.loadIntegrations();
-  }
-  chooseMusicFile(event: Event) {
-    this.musicFile = (event.target as HTMLInputElement).files?.[0] ?? null;
-  }
-  async uploadMusic() {
-    if (!this.musicFile || !this.musicName.trim() || !this.musicLicense.trim()) {
-      return this.flash("Completa nombre, archivo y licencia de la pista.", "error");
-    }
-    await this.withLoading("musicUpload", async () => {
-      try {
-        const form = new FormData();
-        form.append("file", this.musicFile!);
-        form.append("name", this.musicName.trim());
-        form.append("licenseNotes", this.musicLicense.trim());
-        form.append("source", this.musicSource);
-        if (this.musicSource === "ai_generated") form.append("aiProvider", this.musicAiProvider.trim());
-        await this.integrations.uploadMusic(form);
-        this.musicFile = null; this.musicName = ""; this.musicLicense = "";
-        await this.loadIntegrations();
-        this.flash("Pista verificada y guardada.");
-      } catch (error) { this.flash(this.errorText(error), "error"); }
-    });
+  login() {
+    return this.workspace.login(this.email);
   }
-  async connectTikTok() {
-    await this.withLoading("tiktok", async () => {
-      try { await this.integrations.connectTikTok(); }
-      catch (error) { this.flash(this.errorText(error), "error"); }
-    });
-  }
-  async disconnectTikTok() {
-    await this.withLoading("tiktok", async () => {
-      try { await this.integrations.disconnectTikTok(); await this.loadIntegrations(); this.flash("Cuenta TikTok desconectada."); }
-      catch (error) { this.flash(this.errorText(error), "error"); }
-    });
-  }
-  async schedule() {
-    const item = this.selected();
-    if (!item || !this.scheduledFor || !this.musicTrackId)
-      return this.flash("Selecciona fecha, hora y mÃºsica.", "error");
-    await this.withLoading("schedule", async () => {
-      try {
-        await this.api.schedule(item.id, {
-          scheduledFor: new Date(this.scheduledFor).toISOString(),
-          musicTrackId: this.musicTrackId,
-        });
-        await this.load();
-        this.flash("PublicaciÃ³n programada.");
-      } catch (error) {
-        this.flash(this.errorText(error), "error");
-      }
-    });
-  }
-  async approveSelectedBatch() {
-    const batch = this.selectedBatch();
-    if (!batch) return;
-    await this.withLoading("approveBatch", async () => {
-      try {
-        await this.api.approveBatch(batch.id);
-        await this.load();
-        this.selectedBatch.set(
-          this.batches().find((item) => item.id === batch.id) ?? null,
-        );
-        this.flash("Paquete aprobado. Ya puedes programar sus publicaciones.");
-      } catch (error) {
-        this.flash(this.errorText(error), "error");
-      }
-    });
-  }
-  select(item: Publication) {
-    this.selected.set(item);
-  }
-  selectBatch(batch: PublicationBatch) {
-    this.selectedBatch.set(batch);
-  }
-  statusLabel(status: PublicationStatus) {
-    return (
-      {
-        generating: "Generando",
-        pending_review: "Por revisar",
-        approved: "Aprobada",
-        scheduled: "Programada",
-        publishing: "Publicando",
-        published: "Publicada",
-        rejected: "Rechazada",
-        obsolete: "Obsoleta",
-        failed: "Con error",
-      } as Record<string, string>
-    )[status];
-  }
-  batchStatusLabel(status: PublicationBatchStatus) {
-    return (
-      {
-        generating: "Generando",
-        pending_review: "Por revisar",
-        approved: "Aprobado",
-        scheduled: "Programado",
-        published: "Publicado",
-        failed: "Con error",
-      } as Record<string, string>
-    )[status];
-  }
-  imageStyle(item: Publication) {
-    return item.composedImageUrl
-      ? `linear-gradient(180deg,transparent 60%,#08110dc7),url("${item.composedImageUrl}")`
-      : "linear-gradient(145deg,#dca76f,#478187,#183c31)";
-  }
-  openPublication(item: Publication) {
-    this.selected.set(item);
-    this.view.set("publications");
-    this.filter.set(item.status);
-  }
-  private async withLoading<T>(
-    action: AsyncAction,
-    operation: () => Promise<T>,
-  ) {
-    if (this.isLoading(action)) return;
-    this.pendingActions.update((current) => new Set([...current, action]));
-    try {
-      return await operation();
-    } finally {
-      this.pendingActions.update((current) => {
-        const next = new Set(current);
-        next.delete(action);
-        return next;
-      });
-    }
-  }
-  private async consumeTikTokCallback() {
-    const url = new URL(window.location.href);
-    const code = url.searchParams.get("code");
-    const state = url.searchParams.get("state");
-    const error = url.searchParams.get("error_description");
-    if (!code && !error) return;
-    window.history.replaceState({}, document.title, "/");
-    this.view.set("tiktok");
-    if (error) return this.flash(`TikTok rechazÃ³ la conexiÃ³n: ${error}`, "error");
-    if (!state) return this.flash("TikTok no devolviÃ³ un estado de seguridad vÃ¡lido.", "error");
-    try { await this.integrations.exchangeTikTokCode(code!, state); this.flash("Cuenta TikTok conectada correctamente."); }
-    catch (failure) { this.flash(this.errorText(failure), "error"); }
-  }
-  private flash(text: string, kind: MessageKind = "success") {
-    if (this.messageTimer) clearTimeout(this.messageTimer);
-    this.messageKind.set(kind);
-    this.message.set(text);
-    this.messageTimer = setTimeout(() => this.message.set(""), 5500);
-  }
-  private errorText(error: unknown) {
-    if (error instanceof HttpErrorResponse) {
-      const apiMessage =
-        error.error &&
-        typeof error.error === "object" &&
-        typeof error.error.message === "string"
-          ? error.error.message
-          : null;
-      if (apiMessage && error.status < 500) return apiMessage;
-      if (error.status === 0)
-        return "No pudimos conectar con el servidor. Verifica tu conexiÃ³n e intÃ©ntalo nuevamente.";
-      if (error.status === 401)
-        return "Tu sesiÃ³n venciÃ³. Ingresa nuevamente para continuar.";
-      if (error.status >= 500)
-        return "El servicio no estÃ¡ disponible en este momento. IntÃ©ntalo nuevamente en unos minutos.";
-    }
-    return "No pudimos completar la operaciÃ³n. IntÃ©ntalo nuevamente.";
+
+  logout() {
+    return this.workspace.logout();
   }
 }
diff --git a/apps/web/src/app/app.routes.ts b/apps/web/src/app/app.routes.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2885cda47b6742dc13b437043b5263b9add79a05
--- /dev/null
+++ b/apps/web/src/app/app.routes.ts
@@ -0,0 +1,34 @@
+import { Routes } from "@angular/router";
+
+export const routes: Routes = [
+  { path: "", pathMatch: "full", redirectTo: "publications" },
+  {
+    path: "publications",
+    loadChildren: () =>
+      import("./features/publications/publications.routes").then(
+        (module) => module.PUBLICATIONS_ROUTES,
+      ),
+  },
+  {
+    path: "packages",
+    loadChildren: () =>
+      import("./features/packages/packages.routes").then(
+        (module) => module.PACKAGES_ROUTES,
+      ),
+  },
+  {
+    path: "music",
+    loadChildren: () =>
+      import("./features/music/music.routes").then(
+        (module) => module.MUSIC_ROUTES,
+      ),
+  },
+  {
+    path: "settings/tiktok",
+    loadChildren: () =>
+      import("./features/tiktok/tiktok.routes").then(
+        (module) => module.TIKTOK_ROUTES,
+      ),
+  },
+  { path: "**", redirectTo: "publications" },
+];
diff --git a/apps/web/src/app/features/music/music.component.html b/apps/web/src/app/features/music/music.component.html
new file mode 100644
index 0000000000000000000000000000000000000000..3e9f06517d81124bc4d660e2fae91d36986096e3
--- /dev/null
+++ b/apps/web/src/app/features/music/music.component.html
@@ -0,0 +1,47 @@
+<header>
+  <div>
+    <p class="eyebrow">BIBLIOTECA DE AUDIO</p>
+    <h1>MÃºsica para tus publicaciones.</h1>
+    <p>Administra pistas verificadas antes de usarlas en una programaciÃ³n.</p>
+  </div>
+  <button class="refresh" [disabled]="store.busy()" (click)="store.loadIntegrations()">
+    â†» Actualizar
+  </button>
+</header>
+
+<section class="integration-page module-section">
+  <div class="section-title">
+    <div><p class="eyebrow">PISTAS DISPONIBLES</p><h2>MÃºsica verificada</h2></div>
+    <small>{{ store.musicTracks().length }} pistas activas</small>
+  </div>
+  <div class="integration-grid">
+    <article class="panel upload-panel">
+      <h3>AÃ±adir una pista</h3>
+      <p>Por ahora puedes subir mÃºsica original, licenciada o generada con IA. FFprobe valida el formato y la duraciÃ³n antes de habilitarla.</p>
+      <label>Nombre<input [(ngModel)]="store.musicName" placeholder="Ej. Amanecer sereno" /></label>
+      <label>Origen<select [(ngModel)]="store.musicSource"><option value="uploaded">Original o licenciada</option><option value="ai_generated">Generada con IA</option></select></label>
+      @if (store.musicSource === "ai_generated") {
+        <label>Proveedor de IA<input [(ngModel)]="store.musicAiProvider" placeholder="Ej. Mubert, Loudlyâ€¦" /></label>
+      }
+      <label>Licencia / permiso<textarea [(ngModel)]="store.musicLicense" placeholder="Describe el permiso de uso comercial"></textarea></label>
+      <label class="file-field">Archivo MP3, WAV o M4A<input type="file" accept="audio/mpeg,audio/wav,audio/mp4,.m4a" (change)="store.chooseMusicFile($event)" /></label>
+      <button class="primary" [disabled]="store.busy()" [attr.aria-busy]="store.isLoading('musicUpload')" (click)="store.uploadMusic()">
+        @if (store.isLoading("musicUpload")) { <span class="spinner"></span> Verificandoâ€¦ } @else { Verificar y guardar pista }
+      </button>
+    </article>
+
+    <div class="track-list">
+      @for (track of store.musicTracks(); track track.id) {
+        <article class="track-card">
+          <div class="track-icon">â™«</div>
+          <div><strong>{{ track.name }}</strong><small>{{ track.durationSeconds }} s Â· {{ track.source === "ai_generated" ? "IA: " + track.aiProvider : "Original/licenciada" }}</small></div>
+          <span class="verified">âœ“ Verificada</span>
+          @if (track.previewUrl) { <audio controls preload="none" [src]="track.previewUrl"></audio> }
+          <p>{{ track.licenseNotes }}</p>
+        </article>
+      } @empty {
+        <div class="empty">No hay pistas utilizables. Sube la primera para poder programar.</div>
+      }
+    </div>
+  </div>
+</section>
diff --git a/apps/web/src/app/features/music/music.component.ts b/apps/web/src/app/features/music/music.component.ts
new file mode 100644
index 0000000000000000000000000000000000000000..54069aa2f6b8d387f4abb38b7abd072394e3917e
--- /dev/null
+++ b/apps/web/src/app/features/music/music.component.ts
@@ -0,0 +1,18 @@
+import { CommonModule } from "@angular/common";
+import { Component, inject, OnInit } from "@angular/core";
+import { FormsModule } from "@angular/forms";
+import { WorkspaceStore } from "../../workspace.store";
+
+@Component({
+  selector: "app-music",
+  standalone: true,
+  imports: [CommonModule, FormsModule],
+  templateUrl: "./music.component.html",
+})
+export class MusicComponent implements OnInit {
+  readonly store = inject(WorkspaceStore);
+
+  ngOnInit() {
+    void this.store.loadIntegrations();
+  }
+}
diff --git a/apps/web/src/app/features/music/music.routes.ts b/apps/web/src/app/features/music/music.routes.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6ac6da54b184cad150dd8c94204e753196725fd0
--- /dev/null
+++ b/apps/web/src/app/features/music/music.routes.ts
@@ -0,0 +1,11 @@
+import { Routes } from "@angular/router";
+
+export const MUSIC_ROUTES: Routes = [
+  {
+    path: "",
+    loadComponent: () =>
+      import("./music.component").then(
+        (component) => component.MusicComponent,
+      ),
+  },
+];
diff --git a/apps/web/src/app/features/packages/packages.component.html b/apps/web/src/app/features/packages/packages.component.html
new file mode 100644
index 0000000000000000000000000000000000000000..3c2802be9d699cbc2fb99f730d022c90234e4b4b
--- /dev/null
+++ b/apps/web/src/app/features/packages/packages.component.html
@@ -0,0 +1,128 @@
+<header>
+  <div>
+    <p class="eyebrow">PAQUETES GENERADOS</p>
+    <h1>Lotes de imÃ¡genes.</h1>
+    <p>Consulta los paquetes ya generados y abre cada pieza para revisarla.</p>
+  </div>
+  <button
+    class="refresh"
+    [disabled]="store.busy()"
+    [attr.aria-busy]="store.isLoading('load')"
+    (click)="store.load()"
+  >
+    @if (store.isLoading("load")) {
+      <span class="spinner spinner-dark" aria-hidden="true"></span>
+      Actualizandoâ€¦
+    } @else {
+      â†» Actualizar
+    }
+  </button>
+</header>
+
+<section class="packages module-section">
+  <div class="section-title">
+    <div>
+      <p class="eyebrow">HISTORIAL DE LOTES</p>
+      <h2>Paquetes disponibles</h2>
+    </div>
+    <small>{{ store.batches().length }} paquetes</small>
+  </div>
+  <div class="package-board">
+    <div class="package-list">
+      @for (batch of store.batches(); track batch.id) {
+        <button
+          class="package-card"
+          [disabled]="store.busy()"
+          [class.selected]="store.selectedBatch()?.id === batch.id"
+          (click)="store.selectBatch(batch)"
+        >
+          <div>
+            <strong>{{ batch.name }}</strong>
+            <small>{{ batch.createdAt | date: "dd MMM, h:mm a" }}</small>
+          </div>
+          <span class="badge" [attr.data-status]="batch.status">
+            {{ store.batchStatusLabel(batch.status) }}
+          </span>
+          <div class="package-progress">
+            <span
+              [style.width.%]="
+                batch.requestedCount
+                  ? (batch.generatedCount / batch.requestedCount) * 100
+                  : 0
+              "
+            ></span>
+          </div>
+          <small>
+            {{ batch.generatedCount }} de {{ batch.requestedCount }} imÃ¡genes
+            generadas
+          </small>
+        </button>
+      } @empty {
+        <div class="empty">
+          TodavÃ­a no has generado paquetes. CrÃ©alo desde Publicaciones.
+        </div>
+      }
+    </div>
+
+    <article class="package-detail">
+      @if (store.selectedBatch(); as batch) {
+        <div class="review-top">
+          <div>
+            <p class="eyebrow">DETALLE DEL PAQUETE</p>
+            <h2>{{ batch.name }}</h2>
+          </div>
+          <span class="badge" [attr.data-status]="batch.status">
+            {{ store.batchStatusLabel(batch.status) }}
+          </span>
+        </div>
+        <p class="package-description">
+          {{ batch.theme }} Â· {{ batch.tone }} Â·
+          {{ batch.publications.length }} publicaciones
+          @if (batch.obsoleteCount) {
+            Â· {{ batch.obsoleteCount }} versiones obsoletas archivadas
+          }
+        </p>
+        <div class="package-images">
+          @for (item of batch.publications; track item.id) {
+            <button
+              [disabled]="store.busy()"
+              (click)="store.openPublication(item)"
+            >
+              <div
+                class="package-image"
+                [style.background-image]="store.imageStyle(item)"
+              >
+                @if (!item.composedImageUrl) {
+                  <blockquote>â€œ{{ item.quote }}â€</blockquote>
+                }
+              </div>
+              <span>{{ store.statusLabel(item.status) }}</span>
+            </button>
+          }
+        </div>
+        @if (batch.status === "pending_review") {
+          <button
+            class="approve approve-batch"
+            [disabled]="store.busy()"
+            [attr.aria-busy]="store.isLoading('approveBatch')"
+            (click)="store.approveSelectedBatch()"
+          >
+            @if (store.isLoading("approveBatch")) {
+              <span class="spinner" aria-hidden="true"></span> Aprobandoâ€¦
+            } @else {
+              âœ“ Aprobar paquete completo
+            }
+          </button>
+        }
+        <p class="safety">
+          Abre cualquier imagen para aprobarla, rechazarla o regenerarla desde
+          Publicaciones.
+        </p>
+      } @else {
+        <div class="empty review-empty">
+          Selecciona un paquete para ver sus imÃ¡genes.
+        </div>
+      }
+    </article>
+  </div>
+</section>
diff --git a/apps/web/src/app/features/packages/packages.component.ts b/apps/web/src/app/features/packages/packages.component.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2da1394d4794df7996f1233486ee3db05954ed3a
--- /dev/null
+++ b/apps/web/src/app/features/packages/packages.component.ts
@@ -0,0 +1,13 @@
+import { CommonModule } from "@angular/common";
+import { Component, inject } from "@angular/core";
+import { WorkspaceStore } from "../../workspace.store";
+
+@Component({
+  selector: "app-packages",
+  standalone: true,
+  imports: [CommonModule],
+  templateUrl: "./packages.component.html",
+})
+export class PackagesComponent {
+  readonly store = inject(WorkspaceStore);
+}
diff --git a/apps/web/src/app/features/packages/packages.routes.ts b/apps/web/src/app/features/packages/packages.routes.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c78e4f7e61878bd46ad1946cf3a7879c519e6bfa
--- /dev/null
+++ b/apps/web/src/app/features/packages/packages.routes.ts
@@ -0,0 +1,11 @@
+import { Routes } from "@angular/router";
+
+export const PACKAGES_ROUTES: Routes = [
+  {
+    path: "",
+    loadComponent: () =>
+      import("./packages.component").then(
+        (component) => component.PackagesComponent,
+      ),
+  },
+];
diff --git a/apps/web/src/app/features/publications/publications.component.html b/apps/web/src/app/features/publications/publications.component.html
new file mode 100644
index 0000000000000000000000000000000000000000..2ff9df3f3b68080314855f64a282bf3f1b1155d8
--- /dev/null
+++ b/apps/web/src/app/features/publications/publications.component.html
@@ -0,0 +1,291 @@
+<header>
+  <div>
+    <p class="eyebrow">CENTRO DE PUBLICACIONES</p>
+    <h1>Contenido con control humano.</h1>
+    <p>Genera, revisa, aprueba y programa cada publicaciÃ³n desde este mÃ³dulo.</p>
+  </div>
+  <button
+    class="refresh"
+    [disabled]="store.busy()"
+    [attr.aria-busy]="store.isLoading('load')"
+    (click)="store.load()"
+  >
+    @if (store.isLoading("load")) {
+      <span class="spinner spinner-dark" aria-hidden="true"></span>
+      Actualizandoâ€¦
+    } @else {
+      â†» Actualizar
+    }
+  </button>
+</header>
+
+<section class="generator">
+  <div>
+    <span class="step">1</span>
+    <div>
+      <h2>Crear paquete con IA</h2>
+      <p>
+        Este formulario pertenece Ãºnicamente a Publicaciones. Cada imagen
+        aparecerÃ¡ en la bandeja conforme termine de generarse.
+      </p>
+    </div>
+  </div>
+  <div class="fields">
+    <label>
+      Nombre del paquete
+      <input
+        [(ngModel)]="store.batchName"
+        placeholder="Ej. MotivaciÃ³n semanal"
+      />
+    </label>
+    <label class="brand-field">
+      Marca en la imagen
+      <input
+        [(ngModel)]="store.brandName"
+        maxlength="32"
+        placeholder="Ej. Impulso IA"
+      />
+    </label>
+    <label>
+      Tema
+      <input [(ngModel)]="store.theme" />
+    </label>
+    <label>
+      Tono
+      <select [(ngModel)]="store.tone">
+        <option value="cercano">Cercano y positivo</option>
+        <option value="energico">Directo y enÃ©rgico</option>
+        <option value="reflexivo">Reflexivo</option>
+      </select>
+    </label>
+    <label>
+      Cantidad
+      <select [(ngModel)]="store.count">
+        <option [ngValue]="2">2 publicaciones</option>
+        <option [ngValue]="3">3 publicaciones</option>
+      </select>
+    </label>
+    <button
+      class="primary"
+      [disabled]="store.busy()"
+      [attr.aria-busy]="store.isLoading('generate')"
+      (click)="store.generate()"
+    >
+      @if (store.isLoading("generate")) {
+        <span class="spinner" aria-hidden="true"></span> Generando imÃ¡genesâ€¦
+      } @else {
+        âœ¦ Generar paquete
+      }
+    </button>
+  </div>
+</section>
+
+@if (store.generationProgress(); as progress) {
+  <section class="live-generation" aria-live="polite">
+    <div>
+      <span class="spinner spinner-dark"></span>
+      <strong>
+        Creando imagen
+        {{ progress.current + 1 > progress.total ? progress.total : progress.current + 1 }}
+        de {{ progress.total }}
+      </strong>
+    </div>
+    <div class="live-bar">
+      <span [style.width.%]="(progress.current / progress.total) * 100"></span>
+    </div>
+    <small>
+      Los resultados aparecen debajo sin esperar a que concluya todo el lote.
+    </small>
+  </section>
+}
+
+<section class="inbox">
+  <div class="tabs">
+    @for (tab of store.tabs; track tab.value) {
+      <button
+        [disabled]="store.busy()"
+        [class.active]="store.filter() === tab.value"
+        (click)="store.filter.set(tab.value)"
+      >
+        {{ tab.label }} <b>{{ store.countStatus(tab.value) }}</b>
+      </button>
+    }
+  </div>
+
+  <div class="board">
+    <div class="cards">
+      @if (store.loading()) {
+        <div class="empty">Actualizando publicacionesâ€¦</div>
+      }
+      @for (item of store.filtered(); track item.id) {
+        <button
+          class="publication-card"
+          [disabled]="store.busy()"
+          [class.selected]="store.selected()?.id === item.id"
+          (click)="store.select(item)"
+        >
+          <div
+            class="thumb"
+            [style.background-image]="store.imageStyle(item)"
+          >
+            <span class="badge" [attr.data-status]="item.status">
+              {{ store.statusLabel(item.status) }}
+            </span>
+            @if (!item.composedImageUrl) {
+              <blockquote>â€œ{{ item.quote }}â€</blockquote>
+            }
+          </div>
+          <div class="card-copy">
+            <strong>{{ item.theme }}</strong>
+            <small>
+              {{
+                item.scheduledFor
+                  ? (item.scheduledFor | date: "dd MMM, h:mm a")
+                  : "Sin fecha programada"
+              }}
+            </small>
+          </div>
+        </button>
+      } @empty {
+        @if (!store.loading()) {
+          <div class="empty">No hay publicaciones en esta bandeja.</div>
+        }
+      }
+    </div>
+
+    <article class="review">
+      @if (store.selected(); as item) {
+        <div class="review-top">
+          <div>
+            <p class="eyebrow">REVISIÃ“N</p>
+            <h2>{{ store.statusLabel(item.status) }}</h2>
+          </div>
+          <span class="badge" [attr.data-status]="item.status">
+            {{ store.statusLabel(item.status) }}
+          </span>
+        </div>
+        <div class="phone" [style.background-image]="store.imageStyle(item)">
+          @if (!item.composedImageUrl) {
+            <div class="phone-brand">âœ¦ {{ item.brandName }}</div>
+            <blockquote>â€œ{{ item.quote }}â€</blockquote>
+          }
+          <small>â™« MÃºsica seleccionada al programar</small>
+        </div>
+        <label>Frase generada</label>
+        <p class="generated-text">{{ item.quote }}</p>
+        <label>DescripciÃ³n</label>
+        <p class="caption">{{ item.caption }}</p>
+        <div class="hashtags">
+          @for (tag of item.hashtags; track tag) {
+            <span>{{ tag }}</span>
+          }
+        </div>
+
+        @if (item.status === "pending_review") {
+          <div class="actions">
+            <button
+              class="secondary"
+              [disabled]="store.busy()"
+              [attr.aria-busy]="store.isLoading('regenerate')"
+              (click)="store.act('regenerate')"
+            >
+              @if (store.isLoading("regenerate")) {
+                <span class="spinner spinner-dark" aria-hidden="true"></span>
+              } @else {
+                â†» Regenerar
+              }
+            </button>
+            <button
+              class="danger"
+              [disabled]="store.busy()"
+              [attr.aria-busy]="store.isLoading('reject')"
+              (click)="store.act('reject')"
+            >
+              @if (store.isLoading("reject")) {
+                <span class="spinner spinner-dark" aria-hidden="true"></span>
+              } @else {
+                Rechazar
+              }
+            </button>
+            <button
+              class="approve"
+              [disabled]="store.busy()"
+              [attr.aria-busy]="store.isLoading('approve')"
+              (click)="store.act('approve')"
+            >
+              @if (store.isLoading("approve")) {
+                <span class="spinner" aria-hidden="true"></span>
+              } @else {
+                âœ“ Aprobar
+              }
+            </button>
+          </div>
+        }
+
+        @if (item.status === "approved") {
+          <div class="schedule-box">
+            <h3>Programar publicaciÃ³n</h3>
+            <label>
+              Fecha y hora
+              <input type="datetime-local" [(ngModel)]="store.scheduledFor" />
+            </label>
+            <label>
+              MÃºsica
+              <select [(ngModel)]="store.musicTrackId">
+                <option value="">Selecciona una pista licenciada</option>
+                @for (track of store.musicTracks(); track track.id) {
+                  <option [value]="track.id">
+                    {{ track.name }} Â· {{ track.durationSeconds }} s
+                  </option>
+                }
+              </select>
+            </label>
+            @if (store.musicTrackId) {
+              @for (track of store.musicTracks(); track track.id) {
+                @if (track.id === store.musicTrackId && track.previewUrl) {
+                  <audio
+                    class="schedule-audio"
+                    controls
+                    preload="none"
+                    [src]="track.previewUrl"
+                  ></audio>
+                }
+              }
+            }
+            <div class="schedule-checks">
+              <span [class.ok]="store.musicTracks().length">
+                {{ store.musicTracks().length ? "âœ“ MÃºsica verificada" : "! Falta mÃºsica" }}
+              </span>
+              <span [class.ok]="store.tiktok().connected">
+                {{ store.tiktok().connected ? "âœ“ TikTok conectado" : "! Falta TikTok" }}
+              </span>
+            </div>
+            <button
+              class="primary"
+              [disabled]="
+                store.busy() ||
+                !store.tiktok().connected ||
+                !store.musicTracks().length
+              "
+              [attr.aria-busy]="store.isLoading('schedule')"
+              (click)="store.schedule()"
+            >
+              @if (store.isLoading("schedule")) {
+                <span class="spinner" aria-hidden="true"></span> Programandoâ€¦
+              } @else {
+                Programar para TikTok
+              }
+            </button>
+          </div>
+        }
+        <p class="safety">
+          ðŸ”’ Nunca se programa ni publica sin aprobaciÃ³n registrada.
+        </p>
+      } @else {
+        <div class="empty review-empty">
+          Selecciona una publicaciÃ³n para revisarla.
+        </div>
+      }
+    </article>
+  </div>
+</section>
diff --git a/apps/web/src/app/features/publications/publications.component.ts b/apps/web/src/app/features/publications/publications.component.ts
new file mode 100644
index 0000000000000000000000000000000000000000..45265d889f6d83e400eb6edd94b3a182186a41ae
--- /dev/null
+++ b/apps/web/src/app/features/publications/publications.component.ts
@@ -0,0 +1,14 @@
+import { CommonModule } from "@angular/common";
+import { Component, inject } from "@angular/core";
+import { FormsModule } from "@angular/forms";
+import { WorkspaceStore } from "../../workspace.store";
+
+@Component({
+  selector: "app-publications",
+  standalone: true,
+  imports: [CommonModule, FormsModule],
+  templateUrl: "./publications.component.html",
+})
+export class PublicationsComponent {
+  readonly store = inject(WorkspaceStore);
+}
diff --git a/apps/web/src/app/features/publications/publications.routes.ts b/apps/web/src/app/features/publications/publications.routes.ts
new file mode 100644
index 0000000000000000000000000000000000000000..bf2bdf21bbe9e100fa5bfabe825803ab687bb538
--- /dev/null
+++ b/apps/web/src/app/features/publications/publications.routes.ts
@@ -0,0 +1,11 @@
+import { Routes } from "@angular/router";
+
+export const PUBLICATIONS_ROUTES: Routes = [
+  {
+    path: "",
+    loadComponent: () =>
+      import("./publications.component").then(
+        (component) => component.PublicationsComponent,
+      ),
+  },
+];
diff --git a/apps/web/src/app/features/tiktok/tiktok.component.html b/apps/web/src/app/features/tiktok/tiktok.component.html
new file mode 100644
index 0000000000000000000000000000000000000000..31ac8595c4b23ae534b8410a6078b971699c43bd
--- /dev/null
+++ b/apps/web/src/app/features/tiktok/tiktok.component.html
@@ -0,0 +1,31 @@
+<header>
+  <div>
+    <p class="eyebrow">INTEGRACIONES</p>
+    <h1>Cuenta TikTok.</h1>
+    <p>Conecta y administra la cuenta que recibirÃ¡ las publicaciones.</p>
+  </div>
+  <button class="refresh" [disabled]="store.busy()" (click)="store.loadIntegrations()">â†» Actualizar</button>
+</header>
+
+<section class="integration-page module-section">
+  <article class="panel tiktok-panel">
+    <div class="tiktok-mark">â™ª</div>
+    @if (!store.tiktok().configured) {
+      <h3>Falta configurar TikTok en el backend</h3>
+      <p>Agrega TIKTOK_CLIENT_KEY, TIKTOK_CLIENT_SECRET y TIKTOK_REDIRECT_URI. El secret nunca se envÃ­a al navegador.</p>
+    } @else if (store.tiktok().connected) {
+      <span class="verified">âœ“ Conectada</span>
+      <h3>{{ store.tiktok().displayName || "Cuenta TikTok autorizada" }}</h3>
+      <p>La aplicaciÃ³n puede publicar piezas aprobadas. Los tokens se guardan cifrados y se renuevan automÃ¡ticamente.</p>
+      <button class="danger disconnect" [disabled]="store.busy()" [attr.aria-busy]="store.isLoading('tiktok')" (click)="store.disconnectTikTok()">
+        @if (store.isLoading("tiktok")) { <span class="spinner spinner-dark"></span> Desconectandoâ€¦ } @else { Desconectar cuenta }
+      </button>
+    } @else {
+      <h3>Conecta la cuenta que publicarÃ¡</h3>
+      <p>Inicia OAuth con la cuenta de TikTok destino y autoriza los permisos de publicaciÃ³n.</p>
+      <button class="primary" [disabled]="store.busy()" [attr.aria-busy]="store.isLoading('tiktok')" (click)="store.connectTikTok()">
+        @if (store.isLoading("tiktok")) { <span class="spinner"></span> Conectandoâ€¦ } @else { Conectar con TikTok }
+      </button>
+    }
+  </article>
+</section>
diff --git a/apps/web/src/app/features/tiktok/tiktok.component.ts b/apps/web/src/app/features/tiktok/tiktok.component.ts
new file mode 100644
index 0000000000000000000000000000000000000000..65b712a38e733c09ad71d52ad7008dfa883e711d
--- /dev/null
+++ b/apps/web/src/app/features/tiktok/tiktok.component.ts
@@ -0,0 +1,15 @@
+import { Component, inject, OnInit } from "@angular/core";
+import { WorkspaceStore } from "../../workspace.store";
+
+@Component({
+  selector: "app-tiktok",
+  standalone: true,
+  templateUrl: "./tiktok.component.html",
+})
+export class TiktokComponent implements OnInit {
+  readonly store = inject(WorkspaceStore);
+
+  ngOnInit() {
+    void this.store.loadIntegrations();
+  }
+}
diff --git a/apps/web/src/app/features/tiktok/tiktok.routes.ts b/apps/web/src/app/features/tiktok/tiktok.routes.ts
new file mode 100644
index 0000000000000000000000000000000000000000..a99bdcee907e12b49f775db760b2c693e73c616f
--- /dev/null
+++ b/apps/web/src/app/features/tiktok/tiktok.routes.ts
@@ -0,0 +1,12 @@
+import { Routes } from "@angular/router";
+
+export const TIKTOK_ROUTES: Routes = [
+  {
+    path: "",
+    loadComponent: () => import("./tiktok.component").then((component) => component.TiktokComponent),
+  },
+  {
+    path: "callback",
+    loadComponent: () => import("./tiktok.component").then((component) => component.TiktokComponent),
+  },
+];
diff --git a/apps/web/src/app/workspace.store.ts b/apps/web/src/app/workspace.store.ts
new file mode 100644
index 0000000000000000000000000000000000000000..58efd7621f07e3263ef6004510c8c466e5cfe865
--- /dev/null
+++ b/apps/web/src/app/workspace.store.ts
@@ -0,0 +1,465 @@
+import { HttpErrorResponse } from "@angular/common/http";
+import { computed, inject, Injectable, signal } from "@angular/core";
+import { Router } from "@angular/router";
+import type {
+  MusicTrack,
+  Publication,
+  PublicationBatch,
+  PublicationBatchStatus,
+  PublicationStatus,
+  TikTokConnectionStatus,
+} from "@impulso/contracts";
+import { AuthService } from "./auth.service";
+import { IntegrationsApiService } from "./integrations-api.service";
+import { PublicationsApiService } from "./publications-api.service";
+
+export type PublicationFilter = "all" | PublicationStatus;
+export type AsyncAction =
+  | "login"
+  | "logout"
+  | "load"
+  | "generate"
+  | "regenerate"
+  | "reject"
+  | "approve"
+  | "approveBatch"
+  | "schedule"
+  | "musicUpload"
+  | "tiktok";
+type MessageKind = "success" | "error";
+
+@Injectable({ providedIn: "root" })
+export class WorkspaceStore {
+  readonly auth = inject(AuthService);
+  private readonly api = inject(PublicationsApiService);
+  private readonly integrations = inject(IntegrationsApiService);
+  private readonly router = inject(Router);
+
+  readonly items = signal<Publication[]>([]);
+  readonly batches = signal<PublicationBatch[]>([]);
+  readonly loading = signal(false);
+  readonly pendingActions = signal<ReadonlySet<AsyncAction>>(new Set());
+  readonly busy = computed(() => this.pendingActions().size > 0);
+  readonly filter = signal<PublicationFilter>("pending_review");
+  readonly selected = signal<Publication | null>(null);
+  readonly selectedBatch = signal<PublicationBatch | null>(null);
+  readonly musicTracks = signal<MusicTrack[]>([]);
+  readonly tiktok = signal<TikTokConnectionStatus>({
+    configured: false,
+    connected: false,
+  });
+  readonly generationProgress = signal<{ current: number; total: number } | null>(null);
+  readonly message = signal("");
+  readonly messageKind = signal<MessageKind>("success");
+  readonly filtered = computed(() =>
+    this.filter() === "all"
+      ? this.items()
+      : this.items().filter((item) => item.status === this.filter()),
+  );
+
+  batchName = "";
+  brandName = "IMPULSO IA";
+  theme = "MotivaciÃ³n diaria";
+  tone: "cercano" | "energico" | "reflexivo" = "cercano";
+  count: 2 | 3 = 3;
+  scheduledFor = "";
+  musicTrackId = "";
+  musicName = "";
+  musicLicense = "";
+  musicSource: "uploaded" | "ai_generated" = "uploaded";
+  musicAiProvider = "";
+  musicFile: File | null = null;
+  private messageTimer?: ReturnType<typeof setTimeout>;
+  private initialized = false;
+
+  readonly tabs: { value: PublicationFilter; label: string }[] = [
+    { value: "pending_review", label: "Pendientes" },
+    { value: "approved", label: "Aprobadas" },
+    { value: "scheduled", label: "Programadas" },
+    { value: "published", label: "Publicadas" },
+    { value: "failed", label: "Con error" },
+    { value: "obsolete", label: "Obsoletas" },
+    { value: "all", label: "Todas" },
+  ];
+
+  async initializeWorkspace() {
+    if (this.initialized) return;
+    this.initialized = true;
+    await this.consumeTikTokCallback();
+    await Promise.all([this.load(), this.loadIntegrations()]);
+  }
+
+  countStatus(value: PublicationFilter) {
+    return value === "all"
+      ? this.items().length
+      : this.items().filter((item) => item.status === value).length;
+  }
+
+  isLoading(action: AsyncAction) {
+    return this.pendingActions().has(action);
+  }
+
+  async login(email: string) {
+    const normalizedEmail = email.trim();
+    if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(normalizedEmail)) {
+      return this.flash("Ingresa un correo electrÃ³nico vÃ¡lido.", "error");
+    }
+    await this.withLoading("login", async () => {
+      try {
+        await this.auth.sendMagicLink(normalizedEmail);
+        this.flash(
+          "Te enviamos un enlace de acceso. Revisa tu bandeja de entrada y el correo no deseado.",
+        );
+      } catch (error) {
+        this.flash(this.errorText(error), "error");
+      }
+    });
+  }
+
+  async logout() {
+    await this.withLoading("logout", async () => {
+      try {
+        await this.auth.signOut();
+        this.items.set([]);
+        this.batches.set([]);
+        this.selected.set(null);
+        this.selectedBatch.set(null);
+        this.musicTracks.set([]);
+        this.initialized = false;
+      } catch (error) {
+        this.flash(this.errorText(error), "error");
+      }
+    });
+  }
+
+  async load() {
+    await this.withLoading("load", async () => {
+      this.loading.set(true);
+      try {
+        const [rows, batches] = await Promise.all([
+          this.api.list(),
+          this.api.listBatches(),
+        ]);
+        this.items.set(rows);
+        this.batches.set(batches);
+        this.syncSelections(rows, batches);
+      } catch (error) {
+        this.flash(this.errorText(error), "error");
+      } finally {
+        this.loading.set(false);
+      }
+    });
+  }
+
+  async loadIntegrations() {
+    try {
+      const [music, tiktok] = await Promise.all([
+        this.integrations.music(),
+        this.integrations.tiktokStatus(),
+      ]);
+      this.musicTracks.set(music);
+      this.tiktok.set(tiktok);
+      if (!this.musicTrackId && music.length) this.musicTrackId = music[0].id;
+    } catch (error) {
+      this.flash(this.errorText(error), "error");
+    }
+  }
+
+  async generate() {
+    await this.withLoading("generate", async () => {
+      try {
+        let batch = await this.api.generate({
+          name: this.batchName.trim() || undefined,
+          brandName: this.brandName.trim() || "IMPULSO IA",
+          theme: this.theme,
+          tone: this.tone,
+          count: this.count,
+        });
+        this.generationProgress.set({ current: 0, total: this.count });
+        this.batches.update((rows) => [batch, ...rows]);
+        this.selectedBatch.set(batch);
+
+        for (let current = 1; current <= this.count; current++) {
+          const result = await this.api.generateNext(batch.id);
+          batch = result.batch;
+          this.generationProgress.set({ current, total: this.count });
+          this.batches.update((rows) => [
+            batch,
+            ...rows.filter((row) => row.id !== batch.id),
+          ]);
+          this.selectedBatch.set(batch);
+          this.items.update((rows) => [result.publication, ...rows]);
+          this.selected.set(result.publication);
+        }
+
+        this.generationProgress.set(null);
+        await this.load();
+        this.selectedBatch.set(
+          this.batches().find((item) => item.id === batch.id) ?? batch,
+        );
+        this.filter.set("pending_review");
+        this.flash(
+          "La IA creÃ³ las imÃ¡genes del paquete. Revisa cada publicaciÃ³n.",
+        );
+      } catch (error) {
+        this.generationProgress.set(null);
+        this.flash(this.errorText(error), "error");
+      }
+    });
+  }
+
+  async act(action: "approve" | "reject" | "regenerate") {
+    const item = this.selected();
+    if (!item) return;
+    await this.withLoading(action, async () => {
+      try {
+        await this.api[action](item.id);
+        await this.load();
+        this.selected.set(
+          action === "regenerate"
+            ? this.items().find((row) => row.supersedesId === item.id) ??
+                this.items().find((row) => row.id === item.id) ??
+                null
+            : this.items().find((row) => row.id === item.id) ?? null,
+        );
+        this.flash(
+          action === "approve"
+            ? "Contenido aprobado."
+            : action === "reject"
+              ? "Contenido rechazado."
+              : "Nueva versiÃ³n generada.",
+        );
+      } catch (error) {
+        this.flash(this.errorText(error), "error");
+      }
+    });
+  }
+
+  chooseMusicFile(event: Event) {
+    this.musicFile = (event.target as HTMLInputElement).files?.[0] ?? null;
+  }
+
+  async uploadMusic() {
+    if (!this.musicFile || !this.musicName.trim() || !this.musicLicense.trim()) {
+      return this.flash(
+        "Completa nombre, archivo y licencia de la pista.",
+        "error",
+      );
+    }
+    await this.withLoading("musicUpload", async () => {
+      try {
+        const form = new FormData();
+        form.append("file", this.musicFile!);
+        form.append("name", this.musicName.trim());
+        form.append("licenseNotes", this.musicLicense.trim());
+        form.append("source", this.musicSource);
+        if (this.musicSource === "ai_generated") {
+          form.append("aiProvider", this.musicAiProvider.trim());
+        }
+        await this.integrations.uploadMusic(form);
+        this.musicFile = null;
+        this.musicName = "";
+        this.musicLicense = "";
+        await this.loadIntegrations();
+        this.flash("Pista verificada y guardada.");
+      } catch (error) {
+        this.flash(this.errorText(error), "error");
+      }
+    });
+  }
+
+  async connectTikTok() {
+    await this.withLoading("tiktok", async () => {
+      try {
+        await this.integrations.connectTikTok();
+      } catch (error) {
+        this.flash(this.errorText(error), "error");
+      }
+    });
+  }
+
+  async disconnectTikTok() {
+    await this.withLoading("tiktok", async () => {
+      try {
+        await this.integrations.disconnectTikTok();
+        await this.loadIntegrations();
+        this.flash("Cuenta TikTok desconectada.");
+      } catch (error) {
+        this.flash(this.errorText(error), "error");
+      }
+    });
+  }
+
+  async schedule() {
+    const item = this.selected();
+    if (!item || !this.scheduledFor || !this.musicTrackId) {
+      return this.flash("Selecciona fecha, hora y mÃºsica.", "error");
+    }
+    await this.withLoading("schedule", async () => {
+      try {
+        await this.api.schedule(item.id, {
+          scheduledFor: new Date(this.scheduledFor).toISOString(),
+          musicTrackId: this.musicTrackId,
+        });
+        await this.load();
+        this.flash("PublicaciÃ³n programada.");
+      } catch (error) {
+        this.flash(this.errorText(error), "error");
+      }
+    });
+  }
+
+  async approveSelectedBatch() {
+    const batch = this.selectedBatch();
+    if (!batch) return;
+    await this.withLoading("approveBatch", async () => {
+      try {
+        await this.api.approveBatch(batch.id);
+        await this.load();
+        this.selectedBatch.set(
+          this.batches().find((item) => item.id === batch.id) ?? null,
+        );
+        this.flash("Paquete aprobado. Ya puedes programar sus publicaciones.");
+      } catch (error) {
+        this.flash(this.errorText(error), "error");
+      }
+    });
+  }
+
+  select(item: Publication) {
+    this.selected.set(item);
+  }
+
+  selectBatch(batch: PublicationBatch) {
+    this.selectedBatch.set(batch);
+  }
+
+  statusLabel(status: PublicationStatus) {
+    return (
+      {
+        generating: "Generando",
+        pending_review: "Por revisar",
+        approved: "Aprobada",
+        scheduled: "Programada",
+        publishing: "Publicando",
+        published: "Publicada",
+        rejected: "Rechazada",
+        obsolete: "Obsoleta",
+        failed: "Con error",
+      } as Record<string, string>
+    )[status];
+  }
+
+  batchStatusLabel(status: PublicationBatchStatus) {
+    return (
+      {
+        generating: "Generando",
+        pending_review: "Por revisar",
+        approved: "Aprobado",
+        scheduled: "Programado",
+        published: "Publicado",
+        failed: "Con error",
+      } as Record<string, string>
+    )[status];
+  }
+
+  imageStyle(item: Publication) {
+    return item.composedImageUrl
+      ? `linear-gradient(180deg,transparent 60%,#08110dc7),url("${item.composedImageUrl}")`
+      : "linear-gradient(145deg,#dca76f,#478187,#183c31)";
+  }
+
+  async openPublication(item: Publication) {
+    this.selected.set(item);
+    this.filter.set(item.status);
+    await this.router.navigate(["/publications"]);
+  }
+
+  private syncSelections(rows: Publication[], batches: PublicationBatch[]) {
+    const selectedId = this.selected()?.id;
+    const selectedBatchId = this.selectedBatch()?.id;
+    this.selected.set(
+      (selectedId ? rows.find((row) => row.id === selectedId) : undefined) ??
+        rows[0] ??
+        null,
+    );
+    this.selectedBatch.set(
+      (selectedBatchId
+        ? batches.find((row) => row.id === selectedBatchId)
+        : undefined) ??
+        batches[0] ??
+        null,
+    );
+  }
+
+  private async consumeTikTokCallback() {
+    const url = new URL(window.location.href);
+    const code = url.searchParams.get("code");
+    const state = url.searchParams.get("state");
+    const error = url.searchParams.get("error_description");
+    if (!code && !error) return;
+
+    await this.router.navigate(["/settings/tiktok"], { replaceUrl: true });
+    if (error) {
+      return this.flash(`TikTok rechazÃ³ la conexiÃ³n: ${error}`, "error");
+    }
+    if (!state) {
+      return this.flash(
+        "TikTok no devolviÃ³ un estado de seguridad vÃ¡lido.",
+        "error",
+      );
+    }
+    try {
+      await this.integrations.exchangeTikTokCode(code!, state);
+      this.flash("Cuenta TikTok conectada correctamente.");
+    } catch (failure) {
+      this.flash(this.errorText(failure), "error");
+    }
+  }
+
+  private async withLoading<T>(
+    action: AsyncAction,
+    operation: () => Promise<T>,
+  ) {
+    if (this.isLoading(action)) return;
+    this.pendingActions.update((current) => new Set([...current, action]));
+    try {
+      return await operation();
+    } finally {
+      this.pendingActions.update((current) => {
+        const next = new Set(current);
+        next.delete(action);
+        return next;
+      });
+    }
+  }
+
+  private flash(text: string, kind: MessageKind = "success") {
+    if (this.messageTimer) clearTimeout(this.messageTimer);
+    this.messageKind.set(kind);
+    this.message.set(text);
+    this.messageTimer = setTimeout(() => this.message.set(""), 5500);
+  }
+
+  private errorText(error: unknown) {
+    if (error instanceof HttpErrorResponse) {
+      const apiMessage =
+        error.error &&
+        typeof error.error === "object" &&
+        typeof error.error.message === "string"
+          ? error.error.message
+          : null;
+      if (apiMessage && error.status < 500) return apiMessage;
+      if (error.status === 0) {
+        return "No pudimos conectar con el servidor. Verifica tu conexiÃ³n e intÃ©ntalo nuevamente.";
+      }
+      if (error.status === 401) {
+        return "Tu sesiÃ³n venciÃ³. Ingresa nuevamente para continuar.";
+      }
+      if (error.status >= 500) {
+        return "El servicio no estÃ¡ disponible en este momento. IntÃ©ntalo nuevamente en unos minutos.";
+      }
+    }
+    return "No pudimos completar la operaciÃ³n. IntÃ©ntalo nuevamente.";
+  }
+}
diff --git a/apps/web/src/main.ts b/apps/web/src/main.ts
index 800644ee5a044baacd40ddc45ba17571f24e71dc..d8cf944380ffd7570d8601388add9ba6225dbf1f 100644
--- a/apps/web/src/main.ts
+++ b/apps/web/src/main.ts
@@ -1,6 +1,13 @@
 import { bootstrapApplication } from '@angular/platform-browser';
 import { provideHttpClient, withInterceptors } from '@angular/common/http';
+import { provideRouter } from '@angular/router';
 import { AppComponent } from './app/app.component';
+import { routes } from './app/app.routes';
 import { authCookieInterceptor } from './app/auth-cookie.interceptor';
 
-bootstrapApplication(AppComponent, { providers: [provideHttpClient(withInterceptors([authCookieInterceptor]))] }).catch(console.error);
+bootstrapApplication(AppComponent, {
+  providers: [
+    provideHttpClient(withInterceptors([authCookieInterceptor])),
+    provideRouter(routes),
+  ],
+}).catch(console.error);
